spring:
  application:
    name: student-service
  config:
    import:
      - nacos:student-service.yaml?refresh=true
      - nacos:school-common.yaml?refresh=true
  cloud:
    nacos:
      serverAddr: nacos:8848
      discovery:
        enabled: true
    sentinel:
      transport:
        port: 8723
        dashboard: sentinel-dashboard:8858
      eager: true
    openfeign:
      sentinel:
        enabled: true
